const dogJSON = (dogColumns) => {
  const dogJSON = {
    dogBreeds: {
      breed1Total: 10,
      breed1Rank: {},
      breed2Total: 10,
      breed2Rank: {},
    },
  };

  console.log(dogData);
};

export default dogJSON;
